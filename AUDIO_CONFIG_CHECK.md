# 音頻配置檢查清單

本文件用於確認 Hi Lemon 系統的音頻配置是否正確。

## ✅ 當前配置狀態（已優化）

### 🎯 三大關鍵優化已完成

#### 1. I2S Port 完全分流 ✅
- **麥克風 (INMP441)**: I2S_NUM_0
- **喇叭 (MAX98357A)**: I2S_NUM_1
- **狀態**: 硬體路徑完全獨立，互不干擾

#### 2. DMA 緩衝區已加大到 2 秒 ✅
- **配置**: 32 個 × 1024 樣本 = 32768 樣本
- **時長**: 32768 ÷ 16000 Hz = **2.048 秒**
- **AI 推理時間**: 實測 1.1 秒
- **安全餘裕**: 0.9 秒（足夠應對 AI 運算）

#### 3. 能量閾值大幅降低 ✅
- **ENERGY_THRESHOLD**: 100000 → **5000** (降低 20 倍)
- **VOICE_ENERGY_THRESHOLD**: 200000 → **10000** (降低 20 倍)
- **SILENCE_ENERGY_THRESHOLD**: 100000 → **3000** (降低 33 倍)

---

## 🎤 INMP441 麥克風配置

### I2S 配置
```c
// main/hi_lemon_keyword.c
#define I2S_NUM                 I2S_NUM_0
#define I2S_SAMPLE_RATE         16000
#define I2S_BCK_PIN             GPIO_NUM_5
#define I2S_WS_PIN              GPIO_NUM_4
#define I2S_DATA_PIN            GPIO_NUM_6

// DMA 緩衝區配置
.dma_buf_count = 32,     // 32 個緩衝區
.dma_buf_len = 1024,     // 每個 1024 樣本
// 總緩衝 = 32 × 1024 = 32768 樣本 ≈ 2 秒
```

### GPIO 接線
```
INMP441    →    ESP32-S3
─────────────────────────
VDD        →    3.3V
GND        →    GND
L/R        →    GND (左聲道)
WS         →    GPIO 4
SCK        →    GPIO 5
SD         →    GPIO 6
```

---

## 🔊 MAX98357A 音頻放大器配置

### I2S 配置
```c
// main/hi_esp_audio.h
#define I2S_SPEAKER_NUM         I2S_NUM_1  // ✅ 與麥克風分離
#define I2S_SPEAKER_BCK_PIN     GPIO_NUM_8
#define I2S_SPEAKER_WS_PIN      GPIO_NUM_9
#define I2S_SPEAKER_DATA_PIN    GPIO_NUM_10
#define I2S_SPEAKER_SD_PIN      GPIO_NUM_7
#define I2S_SPEAKER_SAMPLE_RATE 16000
```

### GPIO 接線
```
MAX98357A  →    ESP32-S3
─────────────────────────
VIN        →    5V (必須 5V)
GND        →    GND
BCLK       →    GPIO 8
LRC        →    GPIO 9
DIN        →    GPIO 10
SD         →    GPIO 7
GAIN       →    GND (12dB 最大音量)
```

---

## 🎯 能量閾值配置（已優化）

### Edge Impulse 檢測閾值
```c
#define ENERGY_THRESHOLD        5000    // 🔥 降低 20 倍（原 100000）
#define DETECTION_CONFIDENCE    0.6     // 60% 信心度
```

### 錄音觸發閾值
```c
#define VOICE_ENERGY_THRESHOLD  10000   // 🔥 降低 20 倍（原 200000）
#define SILENCE_ENERGY_THRESHOLD 3000   // 🔥 降低 33 倍（原 100000）
```

### 能量值參考表
| 環境 | 能量值範圍 | 是否觸發 |
|------|-----------|---------|
| 完全安靜 | 100-1000 | ❌ 不觸發 |
| 環境噪音 | 1000-3000 | ⚠️ 邊緣 |
| 輕聲說話 | 3000-10000 | ✅ 觸發 |
| 正常說話 | 10000-50000 | ✅ 觸發 |
| 大聲說話 | 50000-500000 | ✅ 觸發 |

---

## 🔄 系統工作流程

### 1. 喚醒詞檢測（持續監聽）
```
麥克風 (I2S_NUM_0) 
  → 1 秒滑動窗口
  → 能量檢查 (> 5000)
  → Edge Impulse AI 推理
  → 檢測到 "Hi Lemon"
```

### 2. 錄音階段
```
檢測到喚醒詞
  → LED 亮起 (GPIO 11)
  → 開始錄音 (1-10 秒)
  → 語音活動檢測 (VAD)
  → 靜音 1.5 秒後自動停止
  → LED 熄滅
```

### 3. 播放階段
```
上傳音頻到服務器
  → 接收 TTS 回應
  → 下載 WAV 文件
  → 喇叭播放 (I2S_NUM_1)
  → 繼續監聽
```

---

## 📊 記憶體配置

### PSRAM 使用（8MB 總容量）
- **Edge Impulse 模型**: ~2MB
- **音頻緩衝區**: ~320KB (10 秒錄音)
- **TTS 下載緩衝**: ~500KB
- **剩餘可用**: ~5MB

### DMA 緩衝區（SRAM）
- **麥克風**: 32 × 1024 × 4 bytes = 128KB
- **喇叭**: 8 × 256 × 2 bytes = 4KB
- **總計**: ~132KB

---

## ✅ 測試檢查清單

### 硬體檢查
- [ ] INMP441 接線正確（特別是 L/R 接 GND）
- [ ] MAX98357A 使用 5V 供電（不是 3.3V）
- [ ] GAIN 引腳接 GND（最大音量）
- [ ] 所有 GND 連接在一起（共地）
- [ ] 喇叭已連接（3-8Ω）

### 軟體檢查
- [x] 麥克風使用 I2S_NUM_0
- [x] 喇叭使用 I2S_NUM_1
- [x] DMA 緩衝區 = 32 × 1024
- [x] ENERGY_THRESHOLD = 5000
- [x] VOICE_ENERGY_THRESHOLD = 10000
- [x] SILENCE_ENERGY_THRESHOLD = 3000

### 功能測試
- [ ] 上電後聽到兩聲測試音（1kHz + 1.5kHz）
- [ ] 說 "Hi Lemon" 能觸發（LED 亮起）
- [ ] 錄音時 LED 保持亮起
- [ ] 說完話 1.5 秒後自動停止（LED 熄滅）
- [ ] 能聽到 TTS 回應

---

## 🐛 故障排除

### 問題 1: 無法觸發 "Hi Lemon"

**症狀**: 說話沒反應，串口沒有檢測日誌

**檢查步驟**:
1. 查看串口輸出的能量值
   ```
   🔊 能量: XXXXX (語音閾值: 5000, 靜音閾值: 3000)
   ```
2. 如果能量值 < 5000，可能是：
   - 麥克風接線錯誤
   - 音量太小
   - 增益不足

**解決方案**:
```c
// 如果能量值持續 < 1000，進一步降低閾值
#define ENERGY_THRESHOLD        1000    // 極度敏感模式
```

### 問題 2: 頻繁誤觸發

**症狀**: 沒說話也觸發錄音

**解決方案**:
```c
// 提高閾值
#define ENERGY_THRESHOLD        10000   // 從 5000 提高到 10000
```

### 問題 3: AI 推理時音頻丟失

**症狀**: 串口顯示 "I2S buffer overflow"

**檢查**:
```c
// 確認 DMA 緩衝區配置
.dma_buf_count = 32,    // 必須 ≥ 32
.dma_buf_len = 1024,    // 必須 ≥ 1024
```

### 問題 4: 喇叭無聲音

**檢查步驟**:
1. 確認 MAX98357A 使用 **5V** 供電（不是 3.3V）
2. 確認 SD 引腳 (GPIO 7) 為 HIGH
3. 確認 GAIN 引腳接 GND
4. 測試喇叭是否正常（用其他設備）

---

## 📈 性能指標

### 延遲時間
- **喚醒詞檢測**: 250ms（滑動窗口）
- **AI 推理**: 1.1 秒
- **錄音開始**: < 100ms
- **TTS 下載**: 2-5 秒（取決於網速）
- **TTS 播放**: 即時

### CPU 使用率
- **待機監聽**: ~30%
- **AI 推理**: ~80%
- **錄音**: ~20%
- **播放**: ~15%

### 記憶體使用
- **PSRAM**: ~3MB / 8MB (37%)
- **SRAM**: ~200KB / 512KB (39%)

---

## 🎓 技術說明

### 為什麼需要 2 秒 DMA 緩衝區？

AI 推理需要 1.1 秒，在這期間：
- CPU 忙於運算，無法及時讀取 I2S 數據
- 如果緩衝區 < 1.1 秒，新數據會覆蓋舊數據
- 2 秒緩衝提供 0.9 秒安全餘裕

### 為什麼降低能量閾值？

原閾值 100000 是為了：
- 避免處理環境噪音
- 節省 CPU 資源

但實際測試發現：
- 正常說話能量只有 10000-50000
- 原閾值導致無法觸發
- 降低到 5000 後觸發率大幅提升

### 為什麼使用兩個 I2S Port？

ESP32-S3 有兩個獨立的 I2S 控制器：
- I2S_NUM_0: 麥克風輸入
- I2S_NUM_1: 喇叭輸出

優點：
- 可以同時錄音和播放
- 避免資源衝突
- 更好的音質

---

**最後更新**: 2024-12-24  
**版本**: v2.0 (已優化)  
**狀態**: ✅ 所有關鍵配置已完成
